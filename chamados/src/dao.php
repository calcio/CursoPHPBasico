<?php
    //refatorar o código
    function inserir ($tabela, $coluna, $valor)
    {
        if (is_array($coluna) && is_array($valor)) {
            if (count($coluna) == count($valor)) {
                # code...
            } else {
                # code...
            }
        } else {
            # code...
        }

    }
